---
import RepoMaturityBadge from './RepoMaturityBadge.astro';
import type { Project } from '@data/projects';

type Props = {
  project: Project;
  thesis?: string;
  coreProof?: string;
};

const { project, thesis, coreProof } = Astro.props;
---

<section class="case-hero">
  <div class="container case-grid">
    <div class="case-copy">
      <p class="section-kicker">Case study</p>
      <h1>{project.title}</h1>
      <p class="lede">{thesis ?? project.summary}</p>
      <div class="hero-meta">
        <span>{project.domain}</span>
        <RepoMaturityBadge maturity={project.maturity} note={project.statusNote} />
      </div>
      {coreProof && <p class="core-proof">Core proof artifact: {coreProof}</p>}
      <div class="button-row">
        <a class="button" href={project.githubUrl}>GitHub Repository</a>
        <a class="button secondary" href="/projects/">All Case Studies</a>
      </div>
    </div>
    <div class="case-image">
      <img src={project.image} alt="" />
    </div>
  </div>
</section>

<style>
  .case-hero {
    padding: 78px 0 42px;
  }

  .case-grid {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(340px, 0.82fr);
    gap: 30px;
    align-items: center;
  }

  h1 {
    max-width: 900px;
    margin: 0;
    color: var(--text-strong);
    font-size: clamp(2.7rem, 6vw, 5.4rem);
    line-height: 0.98;
    letter-spacing: 0;
  }

  .lede {
    max-width: 820px;
    margin: 22px 0 0;
    color: #d8e6f7;
    font-size: clamp(1.06rem, 1.8vw, 1.28rem);
  }

  .hero-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    align-items: center;
    margin-top: 22px;
    color: var(--muted-2);
    font-size: 0.82rem;
    font-weight: 900;
    text-transform: uppercase;
  }

  .button-row {
    margin-top: 28px;
  }

  .core-proof {
    max-width: 760px;
    margin: 18px 0 0;
    border-left: 3px solid var(--amber);
    padding-left: 12px;
    color: #eef6ff;
    font-size: 0.95rem;
    font-weight: 850;
  }

  .case-image {
    border: 1px solid rgba(191, 203, 220, 0.14);
    border-radius: 8px;
    padding: 12px;
    background:
      linear-gradient(180deg, rgba(255, 255, 255, 0.052), rgba(255, 255, 255, 0.018)),
      rgba(7, 13, 23, 0.92);
  }

  img {
    display: block;
    width: 100%;
    border-radius: 8px;
  }

  @media (max-width: 900px) {
    .case-grid {
      grid-template-columns: 1fr;
    }

    .case-image {
      order: -1;
    }
  }
</style>
